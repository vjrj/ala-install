  # Biocache Hub - Records search frontend
services:
  biocache-hub:
    image: ala-hub:{{ biocache_hub_version | default('latest') }}
    container_name: la_biocache-hub
    restart: unless-stopped
    expose:
      - 8080
    environment:
      TZ: {{ server_tz | default('UTC') }}
      JAVA_OPTS: ${BIOCACHE_HUB_JAVA_OPTS}
    depends_on:
      mysql:
        condition: service_healthy
      biocache-service:
        condition: service_healthy
    networks:
      default: {}
      {{ docker_network | default('internal') }}:
    healthcheck:
      test: ["CMD", "sh", "-c", "curl --fail --silent http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
